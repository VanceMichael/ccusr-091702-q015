# 领域约定

馆园共享资产交接网围绕跨场馆与公园的资产流转和环境冻结保存业务事实。主要对象为物品批次、封签、地点、交接、活动预留、环境告警、盘点差异、恢复包。

必须长期遵守以下约束：交接不可跳步且需要双方确认；离线事件不能造成双重位置；环境告警冻结相关流转；批量交接必须原子完成。

时间以带时区的 ISO 8601 字符串交换，服务内部保存 UTC；外部写请求使用调用方提供的幂等键；审计记录采用追加方式保存，不通过覆盖来隐藏历史。